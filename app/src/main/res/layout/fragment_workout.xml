<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bujo_background">

    <!-- Main List (Header handles the scrolling inputs) -->
    <ListView
        android:id="@+id/lv_today_workouts"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@color/bujo_divider"
        android:dividerHeight="1dp" />

    <!-- Secret PR Celebration Overlay -->
    <LinearLayout
        android:id="@+id/layout_celebration"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:background="#DDFFFFFF"
        android:visibility="gone"
        android:orientation="vertical">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🏆"
            android:textSize="80sp"/>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="NEW PERSONAL RECORD!"
            android:textColor="#D4AF37"
            android:textStyle="bold"
            android:textSize="24sp"
            android:layout_marginTop="16dp"/>
    </LinearLayout>
</FrameLayout>