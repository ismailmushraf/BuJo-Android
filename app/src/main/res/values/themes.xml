<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Change the parent to DayNight -->
    <style name="Theme.BuJo" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/black</item>

        <!-- Reference a color resource instead of a hardcoded hex code -->
        <item name="android:windowBackground">@color/bujo_background</item>
    </style>

    <style name="BujoDialog" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>